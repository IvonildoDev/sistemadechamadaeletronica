.html -->
<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Entrega de Equipamento - OS #{{ ordem[0] }}</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;600;700&display=swap">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/style.css') }}">
</head>

<body>
    <div class="container">
        <div class="ordem-header">
            <h1 class="title">Entrega de Equipamento - OS #{{ ordem[0] }}</h1>
            <div class="buttons no-print">
                <button onclick="window.print()" class="btn-primary">
                    <i class="fas fa-print"></i> Imprimir
                </button>
                <a href="{{ url_for('index') }}" class="btn-secondary">
                    <i class="fas fa-arrow-left"></i> Voltar
                </a>
            </div>
        </div>

        <form method="POST" action="{{ url_for('entrega_equipamento', ordem_id=ordem[0]) }}" class="no-print">
            <div class="form-group">
                <h3>Configurar Entrega</h3>
                <div class="form-row">
                    <div class="form-group">
                        <label for="tipo_entrega">Tipo de Entrega:</label>
                        <select id="tipo_entrega" name="tipo_entrega" required>
                            <option value="loja" {% if tipo_entrega=='loja' %}selected{% endif %}>Retirada na Loja
                            </option>
                            <option value="domicilio" {% if tipo_entrega=='domicilio' %}selected{% endif %}>Entrega em
                                Domicílio</option>
                        </select>
                    </div>

                    {% if ordem_status == 'Devolvido sem reparo' %}
                    <div class="form-group">
                        <label for="motivo_devolucao">Motivo da Devolução:</label>
                        <textarea id="motivo_devolucao" name="motivo_devolucao"
                            rows="3">{{ motivo_devolucao }}</textarea>
                    </div>
                    {% endif %}
                </div>

                <div class="buttons">
                    <button type="submit" class="btn-success">
                        <i class="fas fa-check"></i> Confirmar Entrega
                    </button>
                </div>
            </div>
        </form>

        <div class="entrega-status">
            <h2 class="subtitle">Status da Entrega</h2>
            <div class="entrega-status-container">
                <div class="status-item {% if ordem_status == 'Concluído' %}active{% endif %}">
                    <div class="status-icon"><i class="fas fa-check-circle"></i></div>
                    <span>Equipamento Consertado</span>
                </div>
                <div class="status-item {% if ordem_status == 'Devolvido sem reparo' %}active{% endif %}">
                    <div class="status-icon"><i class="fas fa-times-circle"></i></div>
                    <span>Devolução sem Reparo</span>
                </div>
            </div>
        </div>

        <div class="ordem-info">
            <h2 class="subtitle">Informações do Cliente</h2>
            <table>
                <tr>
                    <th>Nome do Cliente:</th>
                    <td>{{ ordem[1] }}</td>
                </tr>
                <tr>
                    <th>Telefone:</th>
                    <td>{{ ordem[12] }}</td>
                </tr>
                <tr>
                    <th>Data de Entrega:</th>
                    <td>{{ data_atual|formatdate }}</td>
                </tr>
            </table>
        </div>

        <div class="ordem-info">
            <h2 class="subtitle">Informações do Equipamento</h2>
            <table>
                <tr>
                    <th>Marca:</th>
                    <td>{{ ordem[2] }}</td>
                </tr>
                <tr>
                    <th>Modelo:</th>
                    <td>{{ ordem[3] }}</td>
                </tr>
                <tr>
                    <th>Número de Série:</th>
                    <td>{{ ordem[4] or 'Não informado' }}</td>
                </tr>
                <tr>
                    <th>Defeito Relatado:</th>
                    <td>{{ ordem[6] }}</td>
                </tr>
            </table>
        </div>

        {% if ordem_status == 'Concluído' and servico %}
        <div class="ordem-info">
            <h2 class="subtitle">Serviço Realizado</h2>
            <table>
                <tr>
                    <th>Diagnóstico:</th>
                    <td>{{ servico.diagnostico }}</td>
                </tr>
                <tr>
                    <th>Descrição do Serviço:</th>
                    <td>{{ servico.descricao_servico }}</td>
                </tr>
                <tr>
                    <th>Serviço Realizado:</th>
                    <td>{{ servico.servico_realizado }}</td>
                </tr>
            </table>
        </div>

        <div class="ordem-info">
            <h2 class="subtitle">Peças Utilizadas</h2>
            {% if servico.produtos and servico.produtos|length > 0 %}
            <table class="tabela-produtos">
                <thead>
                    <tr>
                        <th>Descrição</th>
                        <th>Quantidade</th>
                        <th>Valor Unitário</th>
                        <th>Subtotal</th>
                    </tr>
                </thead>
                <tbody>
                    {% for produto in servico.produtos %}
                    <tr>
                        <td>{{ produto.descricao }}</td>
                        <td>{{ produto.quantidade }}</td>
                        <td>R$ {{ produto.valor }}</td>
                        <td>R$ {{ (produto.quantidade|float * produto.valor|float)|round(2) }}</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
            {% else %}
            <p>Nenhuma peça foi utilizada neste serviço.</p>
            {% endif %}
        </div>

        <div class="resumo-valores">
            <h2 class="subtitle">Resumo de Valores</h2>
            <table>
                <tr>
                    <th>Mão de Obra:</th>
                    <td>R$ {{ servico.valor_mao_obra }}</td>
                </tr>
                {% if servico.produtos and servico.produtos|length > 0 %}
                <tr>
                    <th>Total em Peças:</th>
                    <td>R$ {{ total_pecas|round(2) }}</td>
                </tr>
                {% endif %}
                <tr class="total-row">
                    <th>Valor Total:</th>
                    <td>R$ {{ valor_total|round(2) }}</td>
                </tr>
            </table>
        </div>
        {% endif %}

        <div class="entrega-tipo">
            <h2 class="subtitle">Tipo de Entrega</h2>
            <div class="tipo-container">
                <div class="tipo-item {% if tipo_entrega == 'loja' %}checked{% endif %}">
                    <input type="checkbox" id="entrega_loja" name="tipo_entrega" value="loja" {% if tipo_entrega=='loja'
                        %}checked{% endif %} disabled>
                    <label for="entrega_loja">Retirada na Loja</label>
                </div>
                <div class="tipo-item {% if tipo_entrega == 'domicilio' %}checked{% endif %}">
                    <input type="checkbox" id="entrega_domicilio" name="tipo_entrega" value="domicilio" {% if
                        tipo_entrega=='domicilio' %}checked{% endif %} disabled>
                    <label for="entrega_domicilio">Entrega em Domicílio</label>
                </div>
            </div>
        </div>

        {% if ordem_status == 'Concluído' %}
        <div class="garantia-info">
            <h2 class="subtitle">Informações de Garantia</h2>
            <div class="garantia-content">
                <div class="garantia-icon"><i class="fas fa-shield-alt"></i></div>
                <div class="garantia-text">
                    <p>Este equipamento possui <strong>garantia de 90 dias</strong> a partir da data de entrega
                        ({{ data_atual|formatdate }}), válida até <strong>{{ data_garantia|formatdate }}</strong>.</p>

                    <p>A garantia cobre exclusivamente os defeitos relacionados aos serviços executados e peças
                        substituídas. Não estão cobertos pela garantia:</p>

                    <ul>
                        <li>Danos causados por mau uso, quedas ou acidentes</li>
                        <li>Danos causados por uso inadequado ou em desacordo com as especificações</li>
                        <li>Danos elétricos como sobretensão, descargas atmosféricas ou oscilações de energia</li>
                        <li>Oxidação ou corrosão de componentes</li>
                        <li>Desgaste natural das peças em uso normal</li>
                        <li>Violação ou tentativa de reparo por pessoas não autorizadas</li>
                    </ul>

                    <p>Para acionar a garantia, é necessário apresentar este comprovante de entrega.</p>
                </div>
            </div>
        </div>
        {% else %}
        <div class="devolucao-info">
            <h2 class="subtitle">Informações de Devolução</h2>
            <div class="devolucao-content">
                <div class="devolucao-icon"><i class="fas fa-info-circle"></i></div>
                <div class="devolucao-text">
                    <p>O equipamento está sendo devolvido sem reparo pelos seguintes motivos:</p>
                    <p class="motivo-devolucao">{{ motivo_devolucao }}</p>

                    <p>Ao receber o equipamento, o cliente declara estar ciente das condições de devolução
                        e concorda que não serão realizados serviços no referido aparelho por este estabelecimento.</p>
                </div>
            </div>
        </div>
        {% endif %}

        <div class="declaracao-recebimento">
            <p>Declaro ter recebido o equipamento acima descrito em perfeitas condições, conforme verificado
                no momento da entrega.</p>
        </div>

        <div class="assinaturas">
            <div class="assinatura-cliente">
                <div class="linha-assinatura">___________________________________</div>
                <p>Assinatura do Cliente</p>
            </div>
            <div class="assinatura-atendente">
                <div class="linha-assinatura">___________________________________</div>
                <p>Assinatura do Atendente</p>
            </div>
        </div>
    </div>

    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
</body>

</html>